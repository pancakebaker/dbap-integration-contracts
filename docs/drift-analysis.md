# Current drift analysis

| Contract              | Bidding producer                                | Live Feed consumer                                    | Operations consumer                                        | Laravel client                                           | Discrepancy                                                                           |
| --------------------- | ----------------------------------------------- | ----------------------------------------------------- | ---------------------------------------------------------- | -------------------------------------------------------- | ------------------------------------------------------------------------------------- |
| `BidAccepted`         | Matches v1 fixture and payload record           | Matches required fields and UUID/amount checks        | Deserializes the shared envelope and maps projected fields | Receives the resulting auction/API state through the BFF | None observed                                                                         |
| `AuctionClosed`       | Matches scheduler payload and v1 fixture        | Matches required fields and nullable final bid fields | Maps the event to activity                                 | Observes through Live Feed/BFF surfaces                  | None observed                                                                         |
| `WinnerSelected`      | Matches scheduler payload and v1 fixture        | Matches required fields and UUID/amount checks        | Maps the event to activity                                 | Does not produce or own this event                       | None observed                                                                         |
| `AuctionPurchased`    | Producer record and routing key are present     | Parser and projection support are present             | Mapping and Buy Now visibility are present                 | Sends the Buy Now command through the BFF                | No canonical fixture yet; schema is based on verified source types                    |
| `AuctionCancelled`    | Producer record and routing key are present     | Parser support is present                             | Consumer routing support is present                        | Does not produce this event                              | No canonical fixture yet; schema is based on verified source types                    |
| `TenantStatusChanged` | Shared payload type and routing key are present | Parser supports status and tenant-version checks      | Consumer accepts the envelope and persists activity        | Tenant lifecycle commands use REST, not this event       | No canonical fixture yet; exact producer envelope should be fixture-tested in Phase 2 |

The three carried fixtures are copied byte-for-byte from the standalone Bidding
repository. No application source was copied into this repository.
